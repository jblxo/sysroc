query Projects($userId: String) {
  projects(filter: { user: $userId }) {
    id
    name
    description
    createdAt
    user {
      id
      name
    }
    supervisor {
      id
      name
    }
  }
}
