mutation UpdateProject(
  $name: String!
  $description: String
  $projectId: String!
) {
  updateProject(
    updates: { name: $name, description: $description }
    filter: { _id: $projectId }
  ) {
    id
    name
    description
    user {
      id
    }
  }
}
