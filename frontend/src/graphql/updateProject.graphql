mutation UpdateProject(
  $name: String!
  $description: String
  $projectId: Float!
) {
  updateProject(
    updates: { name: $name, description: $description }
    filter: { id: $projectId }
  ) {
    id
    name
    description
    user {
      id
    }
  }
}
