query Project($_id: String) {
  project(filter: { _id: $_id }) {
    _id
    name
    description
    tasks {
      _id
      name
      description
      createdAt
      dueDate
      completed
    }
  }
}
