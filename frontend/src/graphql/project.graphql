query Project($id: Float) {
  project(filter: { id: $id }) {
    id
    name
    description
    tasks {
      id
      name
      description
      createdAt
      dueDate
      completed
    }
    classifications {
      id
      createdAt
      mark
      note
      user {
        name
      }
    }
    user {
      id
    }
    supervisor {
      id
      name
    }
  }
}
