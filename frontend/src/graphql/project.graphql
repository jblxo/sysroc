query Project($_id: String) {
  project(filter: { _id: $_id }) {
    id
    name
    description
    tasks {
      id
      name
      description
      createdAt
      dueDate
      completed
    }
  }
}
